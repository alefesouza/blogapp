<Page
    x:Class="DropandoIdeias.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DropandoIdeias"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DropandoIdeias.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:LeftPanelViewModel />
    </Page.DataContext>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <SplitView Name="MainSplitView" CompactPaneLength="48" OpenPaneLength="240" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <SplitView.Pane>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ListBox Name="HamburgerLB" Grid.Row="0" SelectionChanged="HamburgerLB_SelectionChanged">
                        <ListBoxItem x:Name="hamburguer">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE700;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="4" />
                                <TextBlock Text="" Margin="18 0 0 0" VerticalAlignment="Center" />
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                    <ListBox Name="HeaderLB" Grid.Row="1" SelectionChanged="HeaderLB_SelectionChanged">
                        <ListBoxItem x:Name="home">
                            <ToolTipService.ToolTip>
                                <TextBlock Text="Início"></TextBlock>
                            </ToolTipService.ToolTip>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="4" />
                                <TextBlock Text="Início" Margin="18 0 0 0" VerticalAlignment="Center" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="favorites">
                            <ToolTipService.ToolTip>
                                <TextBlock Text="Favoritos"></TextBlock>
                            </ToolTipService.ToolTip>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE734;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="4" />
                                <TextBlock Text="Favoritos" Margin="18 0 0 0" VerticalAlignment="Center" />
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                    <ListBox ItemsSource="{x:Bind LeftViewModel.Items}" Name="ListLeftPanel" xmlns:m="using:DropandoIdeias.Models" SelectionChanged="ListLeftPanel_SelectionChanged" BorderThickness="0 1 0 0" BorderBrush="#545454" Grid.Row="2">
                    <ListBox.ItemTemplate>
                        <DataTemplate x:DataType="m:LeftPanelItem">
                            <StackPanel Orientation="Horizontal" ToolTipService.ToolTip="{x:Bind Title}">
                                <TextBlock Text="{x:Bind Icon}" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="4" Visibility="{x:Bind Visibility}" />
                                <Image Source="{x:Bind Image}" Margin="4" VerticalAlignment="Center" Visibility="{x:Bind Visibility2}" />
                                <TextBlock Text="{x:Bind Title}" Margin="18 0 0 0" VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                    <ListBox Name="SettingsLB" SelectionChanged="SettingsLB_SelectionChanged" BorderThickness="0 1 0 0" BorderBrush="#545454" Grid.Row="3">
                        <ListBoxItem x:Name="settings">
                            <ToolTipService.ToolTip>
                                <TextBlock Text="Configurações"></TextBlock>
                            </ToolTipService.ToolTip>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="4" />
                                <TextBlock Text="Configurações" Margin="18 0 0 0" VerticalAlignment="Center" />
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </Grid>
            </SplitView.Pane>

            <SplitView.Content>
                <Frame Name="MainFrame" />
            </SplitView.Content>
        </SplitView>
        
        <Grid x:Uid="Hamburguer" Grid.Row="0">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" >
                <AppBarButton Click="HamburguerButton_Click" Name="CBHamburguer" Width="48" Height="48">
                    <TextBlock Text="&#xE700;" FontFamily="Segoe MDL2 Assets" FontSize="16" HorizontalAlignment="Center" />
                </AppBarButton>
            </StackPanel>
        </Grid>
        
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="MainSplitView.DisplayMode" Value="Overlay" />
                        <Setter Target="MainSplitView.IsPaneOpen" Value="False" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="660" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="MainSplitView.DisplayMode" Value="CompactOverlay" />
                        <Setter Target="MainSplitView.IsPaneOpen" Value="False" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
