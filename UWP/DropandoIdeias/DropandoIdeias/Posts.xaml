<Page
    x:Class="DropandoIdeias.Posts"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DropandoIdeias"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <RelativePanel>
            <ProgressRing Visibility="Collapsed" Name="ToKnow" IsActive="False" />
            
            <ProgressRing IsActive="True" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True" Name="PRing"></ProgressRing>

            <ListView Name="ListPosts" xmlns:m="using:DropandoIdeias.Models" Visibility="Collapsed" SelectionChanged="ListPosts_SelectionChanged">
                <ListView.Header>
                    <Image Name="HeaderImage"></Image>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="m:PostItem">
                        <Grid Margin="5" Background="White">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="{Binding Title}" x:Uid="CardTitle" Margin="10" FontSize="22" TextWrapping="Wrap" Grid.Row="0" />
                            <Image Source="{Binding Image}" Grid.Row="1" />
                            <TextBlock Text="{Binding Description}" Margin="10" Foreground="Black" TextWrapping="Wrap" TextAlignment="Justify" Grid.Row="2" />
                            <StackPanel x:Uid="CardFooter" Padding="15" Grid.Row="3">
                                <TextBlock Text="{Binding Comments}" x:Uid="CardComments" HorizontalAlignment="Center" TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ProgressBar IsIndeterminate="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" Name="PBar"></ProgressBar>
        </RelativePanel>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="ToKnow.IsActive" Value="False" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="848" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="ToKnow.IsActive" Value="True" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>